{
  "schema_version": "agentos.system-config-migration.v1",
  "release": "V0.3.2",
  "purpose": "Reconstruct AgentOS system configuration on a clean database without copying user or business data.",
  "source_of_truth": {
    "runtime_config": "deployment/librechat.yaml",
    "model_reasoning_contract": "deployment/agentos-reasoning-matrix.json",
    "managed_agents": "deployment/agents/*.json",
    "deployment_skills": "deployment/skill/*/SKILL.md",
    "role_policy": "scripts/Sync-AgentOSV032Policy.ps1",
    "agent_publisher": "scripts/Sync-AgentOSAgents.ps1"
  },
  "database_policy": {
    "mode": "fresh_database_plus_declarative_rebuild",
    "included": [
      "ADMIN and USER role capability policy",
      "managed Agent release definitions",
      "Agent marketplace ACL entries generated by synchronization scripts",
      "stable Skill identifiers derived from deployment manifests"
    ],
    "excluded": [
      "users",
      "sessions",
      "conversations",
      "messages",
      "transactions",
      "uploaded files",
      "generated images",
      "audit events",
      "raw MongoDB files"
    ]
  },
  "restore_order": [
    "Generate a private .env from .env.example",
    "Generate deployment/librechat.yaml from private environment values",
    "Start API and MongoDB with a fresh data directory",
    "Register the first administrator through the Web UI",
    "Run scripts/Sync-AgentOSV032Policy.ps1",
    "Run scripts/Sync-AgentOSAgents.ps1",
    "Run scripts/Test-AgentOSV032Gate.ps1"
  ]
}
