name: agentos-demo
version: 1

Use the AgentOS demo capability only for safe, read-only demonstrations.
Choose one of the supported operations: health_check, describe_capabilities, or echo.
Keep payloads short and never invent shell commands, filesystem paths, URLs, credentials, or free-form execution requests.
Explain that the result is a demonstration and has no business side effects.
